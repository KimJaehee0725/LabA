# v0.2.1-staging Validation Report

Date: 2026-05-08
Host scope: staging `/srv/lab-platform`
Deployment commit before this pass: `5d91b2d`
Result: passed after runtime fixes in this report

## Summary

- Validated current wave only: core, edge, Authentik, Gitea, and Plane.
- Deferred app wave remains: MLflow, Nextcloud, Collabora, and Overleaf.
- Smoke artifacts were created with generated credentials, then removed. Secrets and tokens were not written to git/history/report.
- Plane/Auth generic OIDC login remains unresolved and is a mandatory `v0.3.0` release blocker before real user rollout.

## Runtime Fixes From This Pass

- Added Plane Nginx routing for `/auth/` to `plane-api`; without this, Plane login APIs were served by `plane-web` and returned 404.
- Added Plane Nginx routing for `/plane-uploads` to MinIO; without this, Plane v2 presigned browser uploads could not reach the bucket endpoint.
- Added Plane instance bootstrap via `50-bootstrap-plane.sh`; without `is_setup_done`, Plane email/password login returned `INSTANCE_NOT_CONFIGURED`.
- Added a Gitea bootstrap admin env contract and bootstrap step. This prevents the first OIDC `lab-member` login from becoming the only Gitea admin and then failing group-sync demotion.
- Fixed `41-check-gitea.sh` to run `gitea doctor` as container user `git`; running as root is rejected by Gitea.

## Evidence

- Static checks passed:
  - `bash -n deploy/scripts/*.sh deploy/scripts/lib/*.sh`
  - `git diff --check`
  - `docker compose config` for `core`, `edge`, `authentik`, `gitea`, `plane`
  - `nginx -t`
- Runtime checks passed:
  - `04-check-core.sh`
  - `10-check-edge.sh` with Gitea SSH allowed
  - `20-check-authentik.sh`
  - `41-check-gitea.sh`
  - `50-bootstrap-plane.sh`
  - `51-check-plane.sh`
- Security checks passed:
  - Lab stack exposes `80`, `443`, and Gitea SSH `2222`; Postgres, Redis, MinIO, Plane internals, and Gitea web are not published.
  - Docker socket is not mounted by deploy configs.
  - `/srv/lab-platform/nginx/ssl/origin.key` is `0600 root:root`.
  - Anonymous MinIO list requests for checked buckets are denied.

## Practical Smoke

- Authentik:
  - Required groups exist: `lab-admin`, `lab-member`, `lab-collab`, `lab-guest`.
  - Gitea and Plane OIDC discovery endpoints are reachable.
  - Temporary smoke user was created and removed.
- Gitea:
  - Authentik OIDC login created a non-admin Gitea smoke user after bootstrap admin was present.
  - API token with scoped permissions created a private repo.
  - SSH key registration, SSH push, and SSH clone over port `2222` passed.
  - Git LFS batch upload/download passed through Gitea, with object storage verified in MinIO `gitea-lfs`.
- Plane:
  - Local email/password smoke login passed after instance bootstrap.
  - Workspace, project, issue, v2 presigned attachment upload, MinIO object check, and authenticated attachment download passed.
  - Controlled restart of Gitea and Plane containers completed; Gitea repo data and Plane issue/attachment remained readable.

## Cleanup

- Removed temporary Authentik smoke user.
- Removed Gitea smoke user, repo, SSH key, API tokens, and LFS smoke object.
- Removed Plane smoke workspace/project/issue/user and direct-uploaded MinIO object.
- Removed temporary `/tmp/lab-smoke-*` validation files after extracting redacted evidence.
- Kept `gitea-bootstrap-admin` as a non-smoke staging bootstrap admin. Its generated password is stored only in `/srv/lab-platform/env/30-gitea.env` and is not committed.

## Deferred / Blockers

- `v0.3.0` blocker: Plane access must be centrally controlled by Authentik before real users are onboarded.
- Candidate approaches for Plane/Auth:
  - Use a Plane-supported OAuth path if Authentik can emulate it safely.
  - Put Plane behind an Authentik proxy provider while deciding how to handle internal Plane accounts.
  - Upgrade/change Plane edition or patch generic OIDC support.
- Gitea bootstrap admin should be rotated or replaced by a real `lab-admin` SSO account during v0.3 hardening.
