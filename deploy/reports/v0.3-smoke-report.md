# v0.3 Smoke Report

Date: 2026-05-09T10:46:52Z
Host: `jaeheekim-local`
Deployment baseline: `b401a74`

## Summary

- Result: passed for current wave.
- Blockers: none for `core,edge,authentik,gitea,plane,mlflow,nextcloud`.
- Follow-up: Overleaf, Gitea-native document integration, MLflow external programmatic API auth.
- Enabled services: core, edge, authentik, gitea, plane, mlflow, nextcloud.
- Deferred services: overleaf.

## Network And Ports

- Published routes checked through Nginx: Authentik, Gitea, Plane, MLflow, Nextcloud, Collabora.
- MLflow unauthenticated external status: `302`.
- Nextcloud `status.php`: `200`.
- Collabora discovery: `200`.
- Authentik Nextcloud discovery: `200`.
- Unexpected ports: not scanned in this wave.

## Core

- Postgres: passed via `04-check-core.sh`.
- Redis: passed via `04-check-core.sh`.
- MinIO: passed via `04-check-core.sh`.
- Buckets: `mlflow-artifacts` present and artifact object verified by MLflow smoke.

## Nginx

- Authentik route: passed via `20-check-authentik.sh`.
- Gitea route: passed via `41-check-gitea.sh`.
- Plane route: passed via `51-check-plane.sh`.
- MLflow route: passed, unauthenticated request returned `302`.
- Nextcloud route: passed, `status.php` returned `200`.
- Collabora discovery: passed, `/hosting/discovery` returned `200`.
- Overleaf route: deferred.
- MinIO Console route: inherited from current core wave; no new issue found by core check.

## Authentik

- Groups: passed via `20-check-authentik.sh`.
- MFA: not changed in this wave.
- OIDC discovery: passed for `gitea`, `plane`, `nextcloud`.
- Disabled user block: inherited from previous current-wave check; not re-tested here.
- Nextcloud provider signing: RS256 via Authentik signing key.

## App Smoke

- Gitea repo/SSH/LFS: passed via `41-check-gitea.sh` with `ALLOW_GITEA_SSH=true`.
- Plane workspace/item/page/attachment: passed via `51-check-plane.sh`.
- MLflow experiment/run/artifact: passed; latest integrated run ID `f408d9751ebf49e8a8d19f884800b16f`.
- Nextcloud upload/download/Collabora/document hub: passed via `72-check-nextcloud.sh` and browser smoke; manual `.docx` open/save was not performed.
- Nextcloud browser: Files and Collectives reached after Authentik login.
- GitHub integration app/provider presence: `integration_github` enabled.
- Overleaf compile/Git backup: deferred.

## Backup Dry Run

- Postgres: not run in this wave.
- MinIO: not run in this wave.
- Gitea: not run in this wave.
- Nextcloud: not run in this wave.
- Overleaf: deferred.
- Manual Collabora document edit: not run in this wave.

## Security

- Env file permissions: `/srv/lab-platform/env/50-mlflow.env` and `/srv/lab-platform/env/60-nextcloud.env` are `0640`.
- TLS key permissions: no TLS private key was copied from tracked deploy assets during this wave.
- Docker socket mounts: no new Docker socket mount was added by MLflow or Nextcloud compose files.
- Anonymous MinIO: no anonymous artifact access was introduced; MLflow artifact access used the service policy.
- MLflow unauthenticated block: passed; external request returned `302`.
- Secret hygiene: reports/history record only status codes, object names, and non-secret IDs.
