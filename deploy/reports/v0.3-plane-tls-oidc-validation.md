# v0.3 Plane TLS/OIDC Validation Report

Date: 2026-05-09
Host scope: staging `/srv/lab-platform`
Deployment commit before this pass: `8f608e1`
Result: passed for internal CA TLS, Plane `OIDC_VERIFY_SSL=1`, and Plane Authentik browser smoke

## Summary

- Validated the internal CA edge certificate without regenerating `lab-internal-ca.crt`, `origin.crt`, or `origin.key`.
- Confirmed Plane API uses the internal CA bundle and stores `OIDC_VERIFY_SSL=1` in Plane `InstanceConfiguration`.
- Confirmed Authentik OIDC discovery and Plane OIDC initiate path work with the current staging runtime.
- Confirmed a browser login through Authentik reaches the seeded Plane workspace `/lab-demo`.
- Secrets, client secrets, tokens, demo password values, and full env output were not written to this report.

## Certificate Evidence

- `openssl verify -CAfile /srv/lab-platform/nginx/ssl/lab-internal-ca.crt /srv/lab-platform/nginx/ssl/origin.crt`: `OK`
- Issuer: `CN = Lab Platform Internal Root CA, O = Lab Platform`
- Subject: `CN = auth.lab.snu.ac.kr, O = Lab Platform`
- Validity:
  - `notBefore=May  9 00:45:36 2026 GMT`
  - `notAfter=Jun 10 00:45:36 2027 GMT`
- Subject Alternative Names:
  - `auth.lab.snu.ac.kr`
  - `hub.lab.snu.ac.kr`
  - `lab.snu.ac.kr`
  - `mlflow.lab.snu.ac.kr`
  - `files.lab.snu.ac.kr`
  - `office.lab.snu.ac.kr`
  - `papers.lab.snu.ac.kr`
  - `storage.lab.snu.ac.kr`

## Static Validation

- `git diff --check`: passed
- `bash -n deploy/scripts/*.sh deploy/scripts/lib/*.sh`: passed
- Plane compose rendered successfully with a temporary copy of live env files; rendered output was discarded.
- Live Plane compose contains:
  - `REQUESTS_CA_BUNDLE: /run/lab-platform-ca/lab-internal-ca.crt`
  - read-only mount from `${LAB_PLATFORM_ROOT}/nginx/ssl/lab-internal-ca.crt`

## Runtime Validation

- Live `/srv/lab-platform/env/40-plane.env` contains `PLANE_OIDC_VERIFY_SSL=1`.
- Running `plane-api` contains:
  - `OIDC_VERIFY_SSL=1`
  - `REQUESTS_CA_BUNDLE=/run/lab-platform-ca/lab-internal-ca.crt`
  - read-only CA bundle mount at `/run/lab-platform-ca/lab-internal-ca.crt`
- Plane DB `InstanceConfiguration.OIDC_VERIFY_SSL`: `1`
- `docker exec nginx nginx -t`: passed
- `docker exec nginx nginx -s reload`: passed
- TLS-verifying curl checks with `--cacert /srv/lab-platform/nginx/ssl/lab-internal-ca.crt`:
  - Authentik root config: HTTP `200`
  - Authentik Plane discovery: HTTP `200`
  - Plane root: HTTP `200`
- `AUTHENTIK_CHECK_DISCOVERY_SLUGS=plane /srv/lab-platform/scripts/20-check-authentik.sh`: passed
- `/srv/lab-platform/scripts/51-check-plane.sh`: passed

## Browser Smoke

- Ran in `mcr.microsoft.com/playwright:v1.52.0-noble`.
- Used a temporary script and the root-owned `/srv/lab-platform/env/99-demo.env`; demo password value was not printed or persisted.
- Used Playwright `ignoreHTTPSErrors: true` for browser flow because browser trust-store installation for the internal CA is outside this checkpoint. TLS verification itself was covered by OpenSSL and curl checks above.
- Confirmed:
  - Plane login page shows `Continue with Authentik`.
  - Browser reaches the Authentik authorize/login flow.
  - Authentik demo user exists, is active, belongs to `lab-member`, and the configured demo credential authenticates.
  - Browser reaches Plane workspace `/lab-demo`.
- Temporary Playwright files were removed after the smoke.

## Remaining v0.3 Gaps

- MLflow, Nextcloud/Collabora, and Overleaf real env files and containers are not present in the current staging runtime.
- Full v0.3 app smoke for MLflow, Nextcloud/Collabora, Overleaf, and backup dry-run remains pending.
- Public CA or Let's Encrypt edge TLS is outside this checkpoint; current validation is for the internal CA staging path.
