ARG MLFLOW_VERSION=2.20.3
FROM python:3.12-slim

ARG MLFLOW_VERSION

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PIP_NO_CACHE_DIR=1

RUN pip install "mlflow[extras]==${MLFLOW_VERSION}" psycopg2-binary boto3

EXPOSE 5000
